ANSIBLE_VERSION=2.9.2

#Ansible build and deploy variables
ASTERISK_VERSION=$(cat ../../../../modules/asterisk/.package_version)
KAMAILIO_VERSION=$(cat ../../../../modules/kamailio/.package_version)
NGINX_VERSION=$(cat ../../../../modules/nginx/.package_version)
RTPENGINE_VERSION=$(cat ../../../../modules/rtpengine/.rtpengine_version)
VIRTUALENV_VERSION=$(cat ../../../../.omnileads_version)
POSTGRESQL_VERSION=$(cat ../../../../modules/postgresql/.postgresql_version)
REDIS_VERSION=$(cat ../../../../modules/redis/.redis_version)
REDISGEARS_VERSION=$(cat ../../../../modules/redis/.redisgears_version)
WEBSOCKETS_VERSION=$(cat ../../../../modules/websockets/.websockets_version)
WOMBAT_VERSION=20.02.1-272
INSTALL_PREFIX=/opt/omnileads
ASTERISK_LOCATION=/opt/omnileads/asterisk
KAMAILIO_LOCATION=/opt/omnileads/kamailio
VIRTUALENV_LOCATION=$INSTALL_PREFIX/virtualenv
WEBSOCKET_LOCATION=$INSTALL_PREFIX/websockets
USUARIO=omnileads

# Docker Prodenv and Devenv variables
DEVENV_VERSION=1.3.0
############################################
# Variable subnet is necessary for prodenv #
############################################
DOCKER_SUBNET=192.168.15.0/24
