# copy to .env file
# bash> cp .env.template .env
# Them you must to put your AWS credentials. .env file is ignore by git.

AWS_DEFAULT_REGION=us-east-1
AWS_SECRET_ACCESS_KEY=
AWS_ACCESS_KEY_ID=
AWS_BUCKET=fts-public-packages
