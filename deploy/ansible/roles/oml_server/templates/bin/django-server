#!/bin/bash

# solo para ambientes de desarrollo

export DJANGO_SETTINGS_MODULE=ominicontacto.settings.develop

~/virtualenv/bin/python ~/ominicontacto/manage.py runsslserver 0.0.0.0:1210 --certificate /opt/omnileads/nginx_certs/cert.pem --key /opt/omnileads/nginx_certs/key.pem
