- name: Instalacion de Omnileads en SangomaFreePBX
  hosts: omni-freepbx
  roles:
          # Instalación de paquetes y dependencias de omnivoip y setup inicial del server
          - {role: prerequisitos-sangoma, when: ansible_os_family == "Sangoma" }
          # Instalación y configuración de Kamailio
          - {role: kamailio-sangoma, when: ansible_os_family == "Sangoma" }
          # Configuración de asterisk/freepbx para funcionar con Omnileads
          - {role: asterisk-freepbx-sangoma, when: ansible_os_family == "Sangoma" }
          # Seteo de asternic
          - {role: asternic, when: ansible_os_family == "Sangoma" }
